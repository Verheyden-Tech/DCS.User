<dcs:DcsInternPage x:Class="DCS.User.UI.GroupManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dcs="http://schemas.verheyden-tech.com/2025/xaml/presentation"
        xmlns:local="clr-namespace:DCS.User.UI"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        mc:Ignorable="d" d:DataContext="{d:DesignInstance Type=local:GroupViewModel}"
        d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="White">

        <Grid.ContextMenu>
            <ContextMenu Name="GroupManagementContextMenu">
                <MenuItem Header="Gruppe anlegen" Click="CreateNewGroupButton_Click">
                    <MenuItem.Icon>
                        <Image Source="{telerik:RadGlyph Size=1, Type=Image, Glyph=&#xe11e;}" Width="16" Height="16" Stretch="Uniform"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Gruppe bearbeiten" Click="EditGroup_Click">
                    <MenuItem.Icon>
                        <Image Source="{telerik:RadGlyph Size=1, Type=Image, Glyph=&#xe10b;}" Width="16" Height="16" Stretch="Uniform"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Gruppe löschen" Click="DeleteGroup_Click">
                    <MenuItem.Icon>
                        <Image Source="{telerik:RadGlyph Size=1, Type=Image, Glyph=&#xe10c;}" Width="16" Height="16" Stretch="Uniform"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Gruppen generieren" Click="GenerateGroup_Click">
                    <MenuItem.Icon>
                        <Image Source="{telerik:RadGlyph Size=1, Type=Image, Glyph=&#xe13b;}" Width="16" Height="16" Stretch="Uniform"/>
                    </MenuItem.Icon>
                </MenuItem>
            </ContextMenu>
        </Grid.ContextMenu>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <telerik:RadAutoCompleteBox x:Name="SearchUserGroupBox" Grid.Row="0" Grid.Column="0" WatermarkContent="Gruppe suchen.." Margin="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <telerik:RadButton Grid.Row="0" Grid.Column="1" x:Name="CreateNewGroupButton" Content="Neue Gruppe erstellen" CornerRadius="5" Click="CreateNewGroupButton_Click" Margin="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

        <telerik:RadGridView x:Name="GroupGridView" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" ShowGroupPanel="True" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Multiple" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding Ident, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Header="ID" Width="50"/>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Header="Gruppenname" Width="*"/>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding Description, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Header="Beschreibung" Width="*"/>
                <telerik:GridViewCheckBoxColumn DataMemberBinding="{Binding IsActive, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Header="Aktiv" MinWidth="50"/>
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>

    </Grid>

</dcs:DcsInternPage>
