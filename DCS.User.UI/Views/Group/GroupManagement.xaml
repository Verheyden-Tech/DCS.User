<win:DefaultAppControl x:Class="DCS.User.UI.GroupManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:win="clr-namespace:DCS.CoreLib.View;assembly=DCS.CoreLib.View"
        xmlns:local="clr-namespace:DCS.User.UI"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="White">

        <Grid.ContextMenu>
            <ContextMenu Name="GroupManagementContextMenu"/>
        </Grid.ContextMenu>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <telerik:RadAutoCompleteBox x:Name="SearchUserGroupBox" Grid.Row="0" Grid.Column="0" WatermarkContent="Gruppe suchen.." Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <telerik:RadButton Grid.Row="0" Grid.Column="1" x:Name="CreateNewGroupButton" Content="Neue Gruppe erstellen" CornerRadius="5" Click="CreateNewGroupButton_Click" Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

        <GroupBox Header="Verfügbare Gruppen" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <telerik:RadGridView x:Name="GroupGridView" ShowGroupPanel="True" GroupPanelBackground="{x:Null}" BorderBrush="{x:Null}" Margin="5" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                <telerik:RadGridView.Columns>
                    <telerik:GridViewDataColumn DataMemberBinding="{Binding Ident}" Header="ID" MinWidth="70"/>
                    <telerik:GridViewDataColumn DataMemberBinding="{Binding Name}" Header="Gruppenname" MinWidth="150"/>
                    <telerik:GridViewDataColumn DataMemberBinding="{Binding Description}" Header="Beschreibung" MinWidth="200"/>
                    <telerik:GridViewCheckBoxColumn DataMemberBinding="{Binding IsActive}" Header="Aktiv" MinWidth="50"/>
                </telerik:RadGridView.Columns>
            </telerik:RadGridView>
        </GroupBox>

    </Grid>

</win:DefaultAppControl>
